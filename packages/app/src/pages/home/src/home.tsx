import React from 'react';
import { UnorderedList } from './styles';

export const Home: React.FC = () => {
    return (
        <UnorderedList>
            <div>Test</div>
        </UnorderedList>
    );
};

export default Home;
